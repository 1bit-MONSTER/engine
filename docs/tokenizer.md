# Tokenizer

`engine/core/tokenizer.*` reads a byte-level BPE vocabulary from GGUF metadata
(`tokenizer.ggml.*`) and follows the same steps as HF `tokenizers`:

1. **Added-token split.** CONTROL and USER_DEFINED tokens (`<|im_start|>`,
   `<think>` and others) are matched leftmost-longest in the raw text. The
   `parse_special=false` flag turns this off.
2. **NFC normalization.** The GGUF does not record it, but the HF tokenizer
   applies it. Without it, accented or decomposed text encodes differently:
   187 of the 481 test cases change.
3. **Pre-token split** with the Qwen2 regex, ported by hand one branch at a time:
   `(?i:'s|'t|'re|'ve|'m|'ll|'d)|[^\r\n\p{L}\p{N}]?\p{L}+|\p{N}| ?[^\s\p{L}\p{N}]+[\r\n]*|\s*[\r\n]+|\s+(?!\S)|\s+`
4. **Byte-level mapping** (GPT-2 `bytes_to_unicode`), then **BPE merges**:
   the lowest-rank pair merges first, and on a tie the leftmost pair merges first.

`tokenizer.ggml.pre` values other than `qwen2` are refused rather than
approximated. More pre-tokenizers can be added as each gets its own golden.

The Unicode data (letter and number classes, combining classes, decomposition
and composition) is generated by `tools/gen_unicode_tables.py` from Python
`unicodedata` 16.0.0 into `engine/core/unicode_tables.inc`. It has no ICU dependency.

## Verification

`golden_tokenizer` checks three things for each case, all exact against HF
`tokenizers` 0.22.2:

- the pre-token pieces
- the token ids
- the decoded bytes

The pieces are checked separately because BPE can hide a wrong split by
producing the same ids.

| Case set | Cases | Result |
|---|---|---|
| Committed (`tests/golden/qwen3-tokenizer`, runs in CI) | 481 | all match |
| Random, seed 7 (not committed) | 20,081 | all match, 0.9 s |

The fixed cases cover:

- contractions in every position and case
- code, and whitespace runs with `\r\n`
- CJK, Hangul, Arabic, Hebrew, Devanagari and Thai
- decomposed accents and Hangul jamo (NFC)
- emoji ZWJ sequences, flags, and non-breaking and ideographic spaces
- special tokens, including partial ones such as `<|im_start`
- long runs

The random cases mix 18 Unicode blocks with whitespace, contractions and
special tokens.

Mutation check: ten single-rule deletions were tried in the split and folding
code. Nine fail the test. The tenth (the `\p{N}` branch) is an equivalent
mutant: a lone number character falls through to a 1-character match either way.
Examples: the long-s (`ſ`) case fold gives 2 split mismatches, and the ` ?`
before punctuation gives 67.

## Known limits

- BPE is quadratic in the piece length. That is fine for text, but slow on a
  single very long run with no split points.
- Rust `regex` (used by `tokenizers`) may carry a different Unicode version
  than the generated tables. That only matters for characters assigned in the
  newest Unicode versions.
- `decode(skip_special=true)` drops CONTROL tokens. HF's `skip_special_tokens`
  uses its own `special` flag, which differs for a few tokens (for example the
  `<|fim_*|>` tokens are CONTROL in the GGUF but not special in HF).
